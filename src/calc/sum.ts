/**
 * @description: 
 * @param {number} a
 * @param {number} b
 * @return {*}
 */
function sum(a:number,b:number):number{
    return a + b;
}

function reduce(a:number,b:number):number{
    return a - b;
}

export {sum,reduce}